This file must contain information about the submission, including:
  - DBer never mess up
  - Zhao Junwang <zhaojunwang.zjw@alibaba-inc.com>; Li Yang <jianli.ly@alibaba-inc.com>

Use docker to build and run the test.
```
docker build -t dbtest .
docker run --cap-add=SYS_PTRACE --security-opt seccomp=unconfined -v $(pwd):/home/contest/dbtest -d -it --name=dbtest dbtest bash
docker exec -it dbtest bash
```

Compile
```
./complile.sh
```

Run Harness Test
```
#run small workload
./runTestharness.sh

#run public workload
./runTestharness.sh workloads/public
```

TODO:

[x] merge join when the join condition are both sorted, public workload has no merge join predicate
[x] build hash table concurrently, using oneapi::tbb::concurrent_unordered_multimap is very time consuming, this is a bad idea
[] probe concurrently
[x] concurrently execute the queries, use async to do the job
[] USE AVX512 selection to speed up filter scan
[x] split a thread to do filter scan, if the other operator return 0 result, kill the filter scan thread, this is a bad idea, will cause heap-use-after-free exception
